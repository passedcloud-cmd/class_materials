chatGPT, 제미나이는 거대 AI 모델로, RNN 사용
CNN은 지엽적인 정보가 필요한 곳, 특화된 도메인 분야에 사용 
또한 CNN은 실시간 구동/저전력 설계에 특화
스마트 글래스에 들어가는 AI에 트랜스포머는 못 들어가고 CNN만 들어갈 수 있음

또한 실시간 구동/저전력 설계에 특화
스마트 글래스에 들어가는 AI는 트랜스포머는 못 들어가고 CNN만 들어갈 수 있음

FCN 모델
완전 연결층
이미지를 입력으로 받음
이미지 32x32x3
마지막 3은 RGB 색깔 정보 
Flattening: 고차행렬을 한 줄로 나열해서 1차원 벡터로 만듦 
입력층은 Wx라는 함수를 거쳐서 출력을 냄
FCN은 행렬 Wx. 32x32x3 3차원을 1x3072 1차원으로 바꾸는 함수
완전 연결층에서 입력이 이미지가 32x32x3인 고차행렬일 때 Flatten을 거쳐서 1x3072 1차원 벡터로 만드는 것은 이해했습니다. 그런데 10x3072 벡터라는가중치 Wx를 곱해서 10x1 1차원 벡터가 출력이 된다는 것이 이해가 가지 않아요. 변환된 데이터 10개 중 1개로 분류한다는데, 왜 모델상수 W가 10x3072인 거죠. 1x3072가 아닌 이유가 궁금해요 


컨볼루션
이미지 처리 연산 기법
이미지 뿐만 아니라 다른 처리에도 사용됨 
완전히 겹쳐지는 이미지 영역을 하나로 펼치고
W도 하나로 펼쳐서 둘을 내적

깊이/채널은 같아야만 연산을 함
둘을 겹쳐서 연산하기 때문

겹쳐진 영역은 3x5x5 =>x
w는 필터 가중치
b: 편향. 필터에 대한 상수

원하는 타격 위치를 설정해야 함
코너에서 시작하는 건 좋지 않음
겹쳐지지 않은 화소 영역이 있을수 있어서
바운더리 쪽에 있는 화소는 처리하지 않고
확실하게 겹쳐지는 화소만 처리 

합성곱 레이어
필터 1개에서
내적 수행하면 입력이 모두 0이었으니까 출력도 0
0인 부분은 모두 0이니까 보지 않고, 90 * 1/9는 10
90 * 1/9가 2개면 20, 3개면 30, 2개면 20
이미지의 가장자리 영역은 처리하지 않고 넘어감 

보통 필터는 1개가 아니라 여러 개

필터가 6개니까 출력도 활성화맵 6개

입력 대비 출력의 해상도 줄어들음 
입력 해상도 - 필터 해상도 + 1 = 추력 해상도 

입출력 해상도를 유지하고 싶다면 2가지 방법
1) 제로 패딩: 0을 채워 넣음
2) 채우고 싶은데 연산되지 않아 모르는 값을, 가장 가까이에 있는 값을 베껴서 채워넣기
그래서 32가 들어오면 32가 나오도록 함


w와 바이어스가 convolution layer의 상수

맨 앞에 있는 값: 입력 데이터에 몇 개의 이미지가 들어오는지 => 배치 사이즈 N 
출력 배치 Cout
H'과 W'는 패딩을 쓰는가 아닌가에 따라 값이 변할 수 있음 

모델 상수(파라미터)를 증가시키려면?
Conv는 중첩해서 여러번 사용
모델 상수가 많으면 성능이 증가할 거라고 기대했었음 
모델 상수 Conv를 여러 개 두면 
입력에 W1 곱하고 W2 곱하고 W3 곱하고...
한 줄로 곱하면 결국 상수 1개임
행렬 곱셈은 결합법칙이 성립
W3 · W2 · W1 = Wall
레이어를 3개 쌓았는데 사실상 레이어 1개를 쓴 것과 수학적으로 동일함 
파라미터를 3배로 늘려봤자 표현할 수 있는 함수의 종류는 그대로인 것 
선형 레이어만 쌓아봤자 곡선 함수는 표현할 수 없음 
ReLU라는 조건부 스위치를 끼우면 결합법칙이 성립하지 않음. 어떤 지점에서 함수의 방향을 꺾어서 곡선 모양을 흉내낼 수 있음 
ReLU: 음의 값은 0으로, 양의 값은 그대로 보내는 비선형 함수
ReLU를 넣음

필터 시각화
y=Wx
W=3072 
값을 다시 이미지로 바꾸기
y=Wx라는 과정을 통해 대표되는 클래스를 모델링
각 클래스의 대표 템플렛을 구함 
선형 모델의 문제: 다양한 형태의 클래스를 분류하기 어려움
예) 빨간 자동차는 분류할 수 있는데 다른 자동차는 분류하기 어려움
객체의 특징을 반영 못함

FCN
색깔, 위치 등 다양한 템플렛 나옴
여러 레이어를 중첩하고 활성화 함수를 넣음 
성능 개선
더 많은 템플렛을 줌 

CNN
계층별로 다른 이미지
출력에 가까우면 고차원 
입력에 가까우면 저차원
둘은 다른 템플렛을 제공함 
전문적으로 다루는 이미지 영역이 달라짐 
지엽적인 local 차이를 이해하게 함 
이미지를 보고 불량을 검출할 때 FCN보다 CNN이 훨씬 유용
모양이 비슷한 조류 이미지를 분류할 때 - 작은 차이/중간 차이를 구분해야 할 때 CNN이 특화


CNN 모델 구조 - 수용 영역(receptive field)
CNN이 이미지를 처리하면서 한 번에 볼 수 있는 영역의 크기
마지막 레이어가 출력한 레이어 값을 
레이어를 쌓으면 쌓을수록 수용 영역을 넓힐 수 있음 
conv 레이어를 깊게 쌓는 구간
pooling, strid, dilated conv : 해상도를 줄이면서 깊이를 쌓음. 연산의 효율성을 위해

레이어를 쌓을수록 깊이가 증가하는 이유?
입력: 이미지에서 3x3만 받아서 그게 수용 영역의 전부

conv layer2는 입력의 7x7를 다 보고 온 셈
출력값은 입력의 전체를 보고 온 셈 

풀링
해상도가 줄어서 연산도 줄어들음
한 두 픽셀 정도 이동하는 건 무시될 수 있음
해상도를 낮추고 위치 이동에 강건

42쪽
풀링의 결과값은 큰 값만 남기고 결국 해상도를 1/4로 나눈셈

43쪽
사실 정보량 손실 있음 
버리는 것
스트라이드 합성곱이 있으면 s칸씩 이동한 후 출력
풀링을 동시에 수행하는 것 같은 효과
s칸씩 이동하니까 해상도가 줄어드니까
폴링 대신 스트라이드 합성곱 많이 씀

47쪽
10에서 3 빼고 스트라이드 하는 구간으로 나눠서 4가 나옴

49쪽
에러율 낮을수록 성능 좋음 
2012년 처음의 CNN 모델 - 에러율 쭉 내려감
2017년 - 사람보다 에러율이 낮음

AlexNet
8개의 컨블루션 레이어
합성곱 5개 레이어
완전연결 3개 레이어

출력: 1x1024 벡터를 만들고 24개는 버려서 1000개로 씀
one-hot 방식: 1024벡터 중 1개만 1이고 나머지는 0
예측도 원-핫이면 좋겠지만 0~1사이 확률로 나옴

입력 227x227 채널은 3

56쪽 알렉스넷 단계별 구조 
첫 번째: 커널 11x11이(3x11x11) 96개. stride가 4니까 4칸씩 건너뛰어서 연산

그 다음 채널 사이즈 96
오버래핑 max pool

256개의 커널. 96x5x5

다시 오버 래핑
공간 해상도가 13x13이 됨 채널 사이즈는 256ㅐ 

커널 사이즈 384개. 3x3짜리가 384 

384 커널

다시 conv, 커널이 256개
그래서 13x13 채널 256이 됨

마지막 pooling
6x6 채널 256이 나옴

총 9216개 원소가 나옴
FC Wx가 4096/9216
FC는 분류기 역할
출력이 4096

마지막에 1000개(1024로 만들고 24개를 버리는 형식)로 줄이고 softmax 수행
softmax: 모든 활성화 값이 0과  1사이에 있는 값으로 매칭
확률분포로 만들기 위함

출력해상도: 입력 해상도- 커널 +2패딩 나누기 스트라이드 계수 + 1

커널이 96개면 출력 사이즈 96

알렉스넷 연산 비용 계산
출력이 메모리를 얼마나 차지하는지 확인

상수의 개수
3*11*11이 총 96개 있다 
출력 채널 수가 커널 개수니까
커널 하나에 해상도 얼마인가
그걸 다 곱하고 바이어스 웨이트 더하기 
상수 개수가 34944개

연산 비용 계산법

알렉스 넷
연산 상의 특징
메모리 사용은 초기 레이어에 집중 : 초반에 해상도가 높을 테니까.
모델 상수는 연결층 레이어에 집중. FC에 집중됨 
플롭스 자체는, 그러니까 연산은 합성곱에서 발생 
같은 상수가 계속 반복해서 적용되기 때문 

VGGNet
79쪽
VGG-16기준
16은 레이어의 개수를 말함
알렉스 8개짜리보다 더 깊음 - 수용 영역이 넓어짐 

모델 비교 시 맨 위 FC와 Softmax들은 동일
알렉스 넷은 숫자가 지 맘대로 같음

81쪽 
큰 커널을 쓸 필요 없음
작고 단순한 필터를 깊게 쌓는 게 성능 높음

합성곱을 연달아 적용 

합성곱 5x5의 장점 -> 리셉티드 필드가 큼

첫번째 3x3 - 두번째 레이어도 3x3을 적용하면 이미지의 3x3을 넘어서는 영역을 볼 수 있음
3x3을 중첩하면 그 결과 5x5 전체를 볼 수 있음 
수용영역은 같은데 파라미터의 연산 FLOPs가 줄어들음. 더 깊어지고 연산은 줄어들음

중간 메모리를 많이 쓴다 - 중간에서 템플릿을 많이 뽑고 있다
연산량에서 20배 가까이 차이남

88쪽
설계가 단순해서 해석 쉬움
각각의 레이어가 무엇을 의미하는지 알 수 있음
특징 추출기로 활발히 사용
다만 파라미터가 많아서 무거움

ResNet
CNN에서 인기 있는 모델
156개 -> 매우 깊음

ResNET-50 
잔차(Resiual) 블록 - 중요
들어오는 입력을 그대로 출력에 더해줌

배치 정규화 방식 : 10개 레이어 이상을 학습 가능
이상하게 20레이어 보다 56레이어에서 트레이닝 에러가 더 높음
과접합을 했으면 트레이닝 에러가 낮아야 하는데 높아짐 - 트레이닝이 안된다는 것
이걸 해결하기 위해 잔차블록 residual block 넣음
작은 레이어보다 성능이 안 나올 것 같으면 데이터를 그냥 넘기도록 함 
어느 레이어 이상부터는 추가적인 파라미터를 0으로 만듦
정확도가 떨어지는 지점에선 입력값을 그대로 출력으로 보내는 것
결국 입력path가 2개가 됨

보틀넥 잔차 블록 96쪽
1x1짜리를 앞뒤로 넣고 수용영역을 가운데에 
1x1짜리는 들어오는 것을 줄이도록 짤 수 있음
공간 해상도를 줄이거나 늘리거나 함
입력과 출력의 사이즈를 동일하게 28x28x256로 맞출 수 있음
입력과 출력의 사이즈가 같아야 함

97쪽
2개의 깊이였던 걸 3개로 만듦 - 더 깊으니까 연산 성능 좋음

stem 구조
해상도를 평균 내서 채널만 남기는 것 

100쪽
세 모델을 비교
ResNet-34가 VGG-16보다 가벼움-GFLOPs가 3.6임. 근데 에러는 더 적음 
ResNet-18과 34의 블록 설계 차이 \가 있어서 가벼운 정도와 에러 성능은 좀 차이남 

101쪽
100층 이상으로 갈 수 있었던 건 보틀넥 덕분

ResNET 레시피가 정해져있으니 확인 할것 
배치정규화 수행 
렐루 쓰면 He 초기화 적용
드롭 아웃은 FCN에서 많이 쓰는 방법. ResNET에서 쓰지 않음 


모바일넷
구동을 효율적으로 
공간과 채널을 분리
텐서 단위로 처리했던 합성곱과 다름 
하나의 합성곱을 2개의 블록으로 나눔 
그룹 C를 유지하면서, 깊이별로 합성곱을 유지

화소별 합성곱은 Cx1씩 곱을 함 
내부에서 깊이별로 커뮤니케이션
화소들 끼리 커뮤니케이션
그다음 깊이끼리 커뮤니케이션 
2개의 서로 다른 축을 독립적으로 수행해서 토론의 결과가 옆동네에도 전달

연산량이 절감. 9배나 절감

CNN 기반 모델 변천사
AlexNet - VGG - GoogleNet - ResNet - MpbileNet
어케든 동작하는 걸 만들고(alexnet)
효율적이고 깊은 모델
모바일에도 쓸 수 있는 모델(mobilenet)




-------------------------------
오후 수업 

CNN 자체가 지엽적인 특성을 찾는 걸 잘함
픽셀과 픽셀간 의존성, 관계성을 이해하는 게 어려움 

CNN 구조의 장점
정규화

한계: 데이터 순서를 무시
이미지에서도 프레임 순서가 중요할 수 있음
반복되는 패턴이 먼 곳에 존재할 수 있음
하지만 CNN은 멀리 떨어져 있으면 알아채기 어려워함

정보 압축 시 위치 정보 손실

RNN
이전 단계와 이후 단계를 반영
이전 단계를 히든 스테이트에 저장해서 남김
새로운 데이터와 합쳐서 다음으로 넘김
시간, 순서를 반영

원-핫 인코딩
화소를 지칭하는 예) F면 F를 뜻하는 부분만 1, 나머지는 0
벡터화된 데이터에 W를 적용
히든 레이어에 값을 보내면 히든 레이어끼리 영향을 줌
입력이 다음번 히든 레이어에 전달되어 영향을 주고
출력 레이어로도 전달되어 영향을 줌
이전값이 이후 값에 영향을 줌
그래서 시간/순서의 흐름을 반영함

W-xy, W_hh, W_hy
첫 번째 값으로 등장한 char를 다시 입력으로 넣음 
그걸로 나온 출력을 다시 입력으로 넣음
학습 시에 추정이 틀릴 수 있음

원핫 벡터를 W_xh로 은닉 벡터화
이전 은닉에서 현재 은닉으로 
은닉에서 출력으로 

히든 레이어가 이전까지의 문맥을 담고 있음

27페이지

t현재 스테이트 ht는 이전 상태ht-1에 의존
t에 따라 다른 상수W가 있는 게 아님
그러면 오버핏이 발생함
t와 무관한 W를 사용
히든 스테이트는 변하지만 W는 변하지 않는다

ht를 업데이트하는 게 중요함 - Whh와 Wxh만 학습
Whh와 Wxh만 학습하면 됨
tanh : 비선형 변환. 

출력에 Why를 곱해서 최종 출력 yt를 만듦


단일 입력 - 시퀀스 출력
모든 히든 스테이트의 출력을 뽑음

RNN으로 시퀀스 데이터 처리
바닐라 RNN의 한계
기울기 폭발 혹은 소실

기울기는 W의 크기에 비례
기울기가 폭발할 것 같으면 절삭->원래 적용해야할 기울기 값과는 달라짐
기울기 소실은 해결법이 없음 -> 다른 대안 모델 사용

LSTM
히든의 값을 선택적으로 조절 가능
게이트로 조절
게이트 g
입력 게이트도 선발적으로 저장
망각 게이트: 얼마나 많은 정보를 지울 것인지 
출력 게이트: 셀 상태 얼마를 현재 은닉 상태로 출력할지 선발적으로 출력
		필요한 정보만 은닉 상태로 저장

C0~C3 라고 표시된 경로: 하이웨이 path
새로운 연산 결과가 최종 성능에 기여하지 못한다면 0으로 초기화
특정 모듈에서 기울기가 소실되거나 하면 막는 역할
누적 효과로 과거 정보가 희석됨

긴거리 의존성 어텐션/ViT
어텐션 메커니즘
이미지에 노이즈가 있으면 평균값으로 노이즈 부분을 대체
그러면 이미지가 흐릿해짐
그래서 비슷한 픽셀들의 평균값으로 노이즈 부분을 대체

쿼리와 유사한 키를 찾고 싶다면?
어텐션: 쿼리와 키의 비슷한 정도.유사도
어떤 키가 쿼리와 비슷한지 알려줘야 함

어텐션 메커니즘의 상수는 쿼리를 뽑는 상수, 키를 뽑는 상수, value를 뽑는 상수
그리고 어텐션(유사도)

내적을 디멘션으로 나누고 softmax를 수행하면 어텐션 값

쿼리는 모든 패치가 될 수 있음
패치가 다른 패치와 유사한지 알 수 있음

셀프 어텐션
사람이 레이블을 하지않아도 유사한 패치끼리 묶어내는 구조를 만들어냄

교차 어텐션
텍스트와 이미지처럼 이종의 데이터간에도 어텐션 사용 가능
데이터망을 구축 가능 

어텐션 메커니즘
=> supervision이 없어도 됨. 
장기적 맥락에 탁월
데이터 순서를 반영할 수 있는지는?

ViT 위치 인코딩
데이터 순서를 반영하기 위해 위치 인코딩을 적용

각각에 위치 정보를 추가
패치 별 위치 인코딩을 파라미터를 써서 위치값을 학습하게 함
해상도가 바뀌면 재학습을 해야 함. 재학습 안 해도 되긴 하는데 그럼 성능 떨어짐

사인파 인코딩
주파수를 낮은 데서 높은 데까지 올려가면서 긴 형태의 위치 벡터 형태로 출력
그럼 표현력이 좋아짐
x,y로만 표현하면 위치를 표현하기 어려움?
화소 자체에 고주파의 사인 형식을 곱해서 고주파 영역을 넣자
그러면 잃어버리는 데이터가 없음?
고주파에 대응하는 위치 정보를 넣음?

상대 위치 인코딩



MLP head를 통과하는 값들을 쌓기




ViT vs ResNet

ViT 학습 기술 중요
상수가 막대하기 때문에
정규화 기법/데이터 증강 기술이 중요함

ResNet도 성능이 좋아질 수 있음
증강을 많이 하면 도움이 됨
정규화를 넣으면 성능이 더 좋아짐


Swin Transformer 
내부 윈도우를 지정해서 그 지역의 어텐션에 집중함
그 다음에 윈도우를 비껴가게 자르고 내부에서 어텐션 구함

vision transformer


자기 지도 vision 백본
자기지도란 혼자서 알아서 학습했단 뜻


네이티브 통합형 멀티모달 비전 백본은 































